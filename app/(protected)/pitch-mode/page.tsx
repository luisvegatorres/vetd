import { PagePlaceholder } from "@/components/dashboard/page-placeholder"

export default function PitchModePage() {
  return (
    <PagePlaceholder
      title="Pitch Mode"
      description="Full-screen presentation flow for client calls — brand intro, scoped proposal, pricing, and next steps."
    />
  )
}
