import { PagePlaceholder } from "@/components/dashboard/page-placeholder"

export default function PaymentsPage() {
  return (
    <PagePlaceholder
      title="Payments"
      description="Invoices, receipts, and payment status across every client. Connected to Stripe for live updates."
    />
  )
}
