import { PagePlaceholder } from "@/components/dashboard/page-placeholder"

export default function ProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      description="Active builds, sprints, deliverables, and timelines. Everything currently in production for clients."
    />
  )
}
