import { PagePlaceholder } from "@/components/dashboard/page-placeholder"

export default function ClientsPage() {
  return (
    <PagePlaceholder
      title="Clients"
      description="The full client directory — contact info, contracts, project history, and lifetime value at a glance."
    />
  )
}
